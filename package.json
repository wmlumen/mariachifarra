"scripts": {
  "start": "npx live-server --port=3000 --open=/index.html",
  "dev": "npx live-server --port=3000 --open=/index.html --watch=.",
  "build": "mkdir -p public && cp -r * public && rm -rf public/{node_modules,.git,package.json,package-lock.json}",
  "deploy": "npm run build"
}
    "> 1%",
    "last 2 versions",
    "not dead"
  ]
}
